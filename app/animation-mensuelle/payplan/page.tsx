"use client";

import { useEffect, useMemo, useState } from "react";
import styles from "./payplan.module.css";

type Rule={id:string;population:string;jobKey:string;label:string;ruleType:string;thresholds:(number|null)[];coefficients:number[];baseAmountEur:number|null;settings:Record<string,unknown>;sourceReference:string|null};
type Payload={canManage:boolean;version:{id:string;code:string;label:string;effectiveFrom:string;status:string;sourceFilename:string|null;settings:Record<string,unknown>};rules:Rule[]};
function value(v:number|null|undefined){return v==null?"":String(v)}
function populationLabel(key:string){return key==="productif"?"Productifs":key==="chef_equipe"?"Chefs d'équipe":key==="preprod"?"Pré-production / administratif":key==="fixline"?"Fixline":key;}

export default function PayplanPage(){
  const [data,setData]=useState<Payload|null>(null);const [rules,setRules]=useState<Rule[]>([]);const [error,setError]=useState("");const [notice,setNotice]=useState("");const [saving,setSaving]=useState<string>("");
  async function load(){setError("");const response=await fetch("/api/payplan",{cache:"no-store"});const payload=await response.json().catch(()=>({}));if(!response.ok){setError(payload.error||"Payplan indisponible.");return;}setData(payload);setRules(payload.rules??[]);}
  useEffect(()=>{void load();},[]);
  function patch(id:string,fn:(rule:Rule)=>Rule){setRules(current=>current.map(rule=>rule.id===id?fn(rule):rule));}
  async function save(rule:Rule){setSaving(rule.id);setError("");setNotice("");try{const response=await fetch("/api/payplan",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"rule",ruleId:rule.id,thresholds:rule.thresholds,coefficients:rule.coefficients,baseAmountEur:rule.baseAmountEur,settings:rule.settings})});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||"Enregistrement impossible.");setNotice(`${rule.label} enregistré dans le Payplan actif.`);await load();}catch(e){setError(e instanceof Error?e.message:"Enregistrement impossible.");}finally{setSaving("");}}
  const groups=useMemo(()=>[...new Set(rules.map(r=>r.population))],[rules]);
  if(!data&&!error)return <main className={styles.page}><div className={styles.shell}>Chargement du Payplan…</div></main>;
  return <main className={styles.page}><header className={styles.header}><div><a href="/animation-mensuelle">← ANIMATION MENSUELLE</a><span>PARAMÉTRAGE FINANCIER VERSIONNÉ</span><h1>Payplan</h1><p>Les seuils, coefficients et bases € définissent le moteur du prochain workflow. À l'ouverture d'un mois, cette version est copiée puis figée : modifier le Payplan plus tard ne recalculera jamais un mois déjà ouvert.</p></div>{data&&<div className={`${styles.badge} ${!data.canManage?styles.readonly:""}`}>{data.canManage?"CYRIL · MODIFICATION AUTORISÉE":"LECTURE SEULE"}</div>}</header><div className={styles.shell}>{error&&<div className={styles.error}>{error}</div>}{notice&&<div className={styles.notice}>{notice}</div>}{data&&<><section className={styles.version}><div><span>Version active</span><strong>{data.version.code}</strong></div><div><span>Date d'effet</span><strong>{new Date(data.version.effectiveFrom).toLocaleDateString("fr-FR")}</strong></div><div><span>Source initiale</span><strong>{data.version.sourceFilename??"KPI CRVO"}</strong></div><div><span>Statut</span><strong>{data.version.status.toUpperCase()}</strong></div></section>{groups.map(group=><section className={styles.group} key={group}><div className={styles.groupHead}><div><span className={styles.eyebrow}>RÈGLES MÉTIER</span><h2>{populationLabel(group)}</h2></div><span className={styles.eyebrow}>{rules.filter(r=>r.population===group).length} MÉTIERS</span></div><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Métier</th><th>P1</th><th>P2</th><th>P3</th><th>P4</th><th>P5</th><th>Coef P1</th><th>Coef P2</th><th>Coef P3</th><th>Coef P4</th><th>Coef P5</th><th>Base €</th><th>Action</th></tr></thead><tbody>{rules.filter(r=>r.population===group).map(rule=><tr key={rule.id}><td className={styles.label}><strong>{rule.label}</strong><small>{rule.ruleType} · {rule.sourceReference??"KPI"}</small></td>{[0,1,2,3,4].map(index=><td key={`t-${index}`}><input disabled={!data.canManage} value={value(rule.thresholds[index])} onChange={e=>patch(rule.id,r=>{const next=[...r.thresholds];next[index]=e.target.value===""?null:Number(e.target.value);return{...r,thresholds:next};})}/></td>)}{[0,1,2,3,4].map(index=><td key={`c-${index}`}><input disabled={!data.canManage} value={value(rule.coefficients[index])} onChange={e=>patch(rule.id,r=>{const next=[...r.coefficients];next[index]=Number(e.target.value);return{...r,coefficients:next};})}/></td>)}<td><input className={styles.base} disabled={!data.canManage} value={value(rule.baseAmountEur)} placeholder="à définir" onChange={e=>patch(rule.id,r=>({...r,baseAmountEur:e.target.value===""?null:Number(e.target.value)}))}/></td><td><button disabled={!data.canManage||saving===rule.id} onClick={()=>void save(rule)}>{saving===rule.id?"…":"ENREGISTRER"}</button></td></tr>)}</tbody></table></div></section>)}<section className={styles.group}><div className={styles.groupHead}><div><span className={styles.eyebrow}>POLITIQUE DU MOTEUR</span><h2>Paramètres hérités du fichier de juillet</h2></div></div><div className={styles.settings}><div className={styles.setting}><span>Heures standard / jour</span><strong>{String(data.version.settings.standardHoursPerDay??7.5)} h</strong></div><div className={styles.setting}><span>Chefs d'équipe / jour</span><strong>{String(data.version.settings.teamManagerHoursPerDay??7.8)} h</strong></div><div className={styles.setting}><span>Collectif</span><strong>{data.version.settings.collectivePolicyNeedsConfirmation?"Politique à confirmer avant clôture native":"Politique validée"}</strong></div></div><p className={styles.footer}>Les bases € restent volontairement vides tant qu'une source contractuelle fiable n'est pas renseignée. C'est un contrôle bloquant : KPI CRVO ne clôturera pas un workflow natif avec un montant de référence manquant.</p></section></>}</div></main>;
}
