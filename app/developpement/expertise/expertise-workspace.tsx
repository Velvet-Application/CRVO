"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { EXPERTISE_FORFAITS, GRADE_RULES, REPAIR_METHODS, type ExpertiseForfait, type GradeKey } from "./expertise-data";
import styles from "./expertise-workspace.module.css";

type Mode="desktop"|"mobile";
type Vehicle={registration:string|null;workOrder:string|null;client:string|null;vin:string|null;model:string|null;mileage:number;status:string;statusAgeDays:number;factoryAgeDays:number;alert?:string|null;urgency?:string|null;location:string|null;site?:string|null;manufacturer?:string|null;folderNumber?:string|null;sourceType:string;processProfile:"EFF"|"BMW"|"AUTRE"|"EXCLU";inFactory:boolean};
type ProductionPayload={connected:boolean;sourceModifiedAt:string|null;vehicles:Vehicle[];error?:string};
type MediaRef={id:string;name:string;type:string;dataUrl:string;createdAt:string};
type Tyre={position:"AVG"|"AVD"|"ARG"|"ARD";brand:string;dimension:string;treadMm:string;status:""|"OK"|"SURVEILLER"|"REMPLACER";runflat:boolean};
type Damage={id:string;zone:string;zoneLevel:""|"HAUTE"|"BASSE";type:string;description:string;sizeCm:string;method:string;paintFinish:""|"AVEC_DIFF"|"SANS_DIFF";forfaitId:string;manualLabel:string;manualHt:string;photos:MediaRef[]};
type RecordData={vehicleKey:string;grade:GradeKey|null;general:{maintenanceHistory:""|"OUI"|"NON"|"INCONNU";historyComment:string;generalComment:string;exteriorPhotos:MediaRef[];interiorPhotos:MediaRef[];maintenanceDocuments:MediaRef[];otherDocuments:MediaRef[]};mechanics:{category:""|"A"|"B"|"C";serviceDue:""|"OUI"|"NON";serviceForfaitId:string;tyres:Tyre[];tyreForfaitId:string;brakeFront:""|"OK"|"PLAQUETTES"|"DISQUES_PLAQUETTES";brakeRear:""|"OK"|"PLAQUETTES"|"DISQUES_PLAQUETTES";brakeFrontForfaitId:string;brakeRearForfaitId:string;shockFront:""|"OK"|"FUITE"|"JEU"|"REMPLACER";shockRear:""|"OK"|"FUITE"|"JEU"|"REMPLACER";shockManualHt:string;notes:string};damages:Damage[];clientOptions:Record<string,boolean>;updatedAt:string};
type QuoteItem={key:string;category:string;label:string;defect:string;justification:string;method:string;photoData:string|null;amountHt:number;amountTtc:number;clientSelectable:boolean;clientChoice?:boolean|null};
type WorkflowCase={id:string;vehicleKey:string;status:string;shareToken:string;currentRevision:number;totalHt:number;totalTtc:number;validatedAt?:string|null;submittedAt?:string|null;firstOpenedAt?:string|null;clientDecidedAt?:string|null;createdAt?:string;updatedAt?:string};
type WorkflowEvent={id:number;type:string;actorRole:string;actorName:string|null;details:Record<string,unknown>;createdAt:string};
type WorkflowMessage={id:number;authorRole:"expert"|"client";authorName:string|null;body:string;createdAt:string};
type WorkflowPayload={connected?:boolean;case?:WorkflowCase|null;draftSnapshot?:{record?:Partial<RecordData>}|null;snapshot?:unknown;items?:QuoteItem[];events?:WorkflowEvent[];messages?:WorkflowMessage[];error?:string};
type Step="general"|"mechanics"|"body"|"quote";
type ElementFamily="wheel"|"glass"|"mirror"|"bumper"|"roof"|"simple"|"complex"|"interior"|"other";

const TYRES:Tyre["position"][]=["AVG","AVD","ARG","ARD"];
const BODY_ZONES=["Bouclier AV","Capot","Aile AVG","Porte AVG","Porte ARG","Aile ARG","Hayon / coffre","Aile ARD","Porte ARD","Porte AVD","Aile AVD","Bouclier AR","Pavillon","Pare-brise","Rétroviseur AVG","Rétroviseur AVD","Jante AVG","Jante AVD","Jante ARG","Jante ARD","Intérieur"] as const;
const METHOD_LABELS:Record<string,string>=Object.fromEntries([...REPAIR_METHODS.map(item=>[item.id,item.label]),["LUSTRAGE","Lustrage / correction légère"],["REMPLACEMENT","Remplacement"],["SURVEILLANCE","Surveillance / pas de travaux"],["HORS_FORFAIT","Hors forfait"]]);
const STEPS:Array<{key:Step;label:string}>=[{key:"general",label:"01 · Dossier"},{key:"mechanics",label:"02 · Mécanique"},{key:"body",label:"03 · Carrosserie"},{key:"quote",label:"04 · Devis & client"}];

function vehicleKey(v:Vehicle){return String(v.vin||v.registration||v.workOrder||`${v.client}-${v.model}`);}
function money(v:number){return new Intl.NumberFormat("fr-FR",{style:"currency",currency:"EUR",minimumFractionDigits:2}).format(v||0);}
function dt(v?:string|null){if(!v)return"—";const d=new Date(v);return Number.isNaN(d.getTime())?"—":new Intl.DateTimeFormat("fr-FR",{day:"2-digit",month:"2-digit",year:"numeric",hour:"2-digit",minute:"2-digit",second:"2-digit",timeZone:"Europe/Paris"}).format(d);}
function num(v:unknown){const n=Number(String(v??"").replace(",","."));return Number.isFinite(n)?Math.max(0,n):0;}
function emptyTyres():Tyre[]{return TYRES.map(position=>({position,brand:"",dimension:"",treadMm:"",status:"",runflat:false}));}
function emptyRecord(key:string):RecordData{return{vehicleKey:key,grade:null,general:{maintenanceHistory:"",historyComment:"",generalComment:"",exteriorPhotos:[],interiorPhotos:[],maintenanceDocuments:[],otherDocuments:[]},mechanics:{category:"",serviceDue:"",serviceForfaitId:"",tyres:emptyTyres(),tyreForfaitId:"",brakeFront:"",brakeRear:"",brakeFrontForfaitId:"",brakeRearForfaitId:"",shockFront:"",shockRear:"",shockManualHt:"",notes:""},damages:[],clientOptions:{},updatedAt:new Date().toISOString()};}
function normalizeRecord(value:Partial<RecordData>|undefined,key:string):RecordData{const base=emptyRecord(key);if(!value)return base;return{...base,...value,vehicleKey:key,general:{...base.general,...value.general},mechanics:{...base.mechanics,...value.mechanics,tyres:Array.isArray(value.mechanics?.tyres)&&value.mechanics!.tyres.length?value.mechanics!.tyres:base.mechanics.tyres},damages:Array.isArray(value.damages)?value.damages:[],clientOptions:value.clientOptions??{},updatedAt:value.updatedAt??base.updatedAt};}
function familyForZone(zone:string):ElementFamily{if(/^Jante/i.test(zone))return"wheel";if(/Pare-brise/i.test(zone))return"glass";if(/Rétroviseur/i.test(zone))return"mirror";if(/Bouclier/i.test(zone))return"bumper";if(/Pavillon/i.test(zone))return"roof";if(/Capot|Aile AR/i.test(zone))return"complex";if(/Aile AV|Porte|Hayon/i.test(zone))return"simple";if(/Intérieur/i.test(zone))return"interior";return"other";}
function damageTypesForZone(zone:string){const f=familyForZone(zone);if(f==="wheel")return["Rayure / frottement","Coup / choc","Déformation / voile","Vernis / peinture endommagé","Cassée / irréparable"];if(f==="glass")return["Impact","Fissure","Multi-impact","Rayure vitrage","Autre défaut vitrage"];if(f==="mirror")return["Rayure / griffe","Coup / choc","Coquille cassée","Glace endommagée","Clignotant / équipement endommagé"];if(f==="interior")return["Tache / salissure","Griffe","Déchirure","Brûlure","Élément cassé / manquant"];if(f==="bumper")return["Rayure / griffe","Coup / enfoncement","Bosse","Impact / gravillonnage","Fissure","Déchirure / percement"];return["Rayure / griffe","Coup / enfoncement","Bosse","Impact / gravillonnage","Déformation","Éclat peinture","Corrosion"];}
function methodsForZone(zone:string){const f=familyForZone(zone);if(f==="wheel")return["JANTE","REMPLACEMENT","SURVEILLANCE","HORS_FORFAIT"];if(f==="glass")return["PARE_BRISE","REMPLACEMENT","SURVEILLANCE","HORS_FORFAIT"];if(f==="interior")return["REMPLACEMENT","SURVEILLANCE","HORS_FORFAIT"];if(f==="mirror")return["LUSTRAGE","PEINTURE","PLUME","REMPLACEMENT","SURVEILLANCE","HORS_FORFAIT"];return["LUSTRAGE","DSP","PEINTURE","PLUME","REMPLACEMENT","SURVEILLANCE","HORS_FORFAIT"];}
function paintForfaitIds(zone:string){const f=familyForZone(zone);if(f==="roof")return["forfait-55","forfait-58"];if(f==="mirror")return["forfait-56","forfait-58"];if(f==="complex")return["forfait-51","forfait-54","forfait-58"];if(f==="bumper")return["forfait-51","forfait-52","forfait-53","forfait-57","forfait-58"];if(f==="simple")return["forfait-51","forfait-52","forfait-53","forfait-58"];return[];}
function forfaitById(id:string){return EXPERTISE_FORFAITS.find(f=>f.id===id)||null;}
function forfaitsForDamage(d:Damage):ExpertiseForfait[]{if(d.method==="JANTE")return EXPERTISE_FORFAITS.filter(f=>f.section==="JANTE");if(d.method==="PARE_BRISE")return EXPERTISE_FORFAITS.filter(f=>f.id==="forfait-50");if(d.method==="DSP")return EXPERTISE_FORFAITS.filter(f=>f.section.trim()==="DSP");if(d.method==="PLUME")return EXPERTISE_FORFAITS.filter(f=>f.id==="forfait-59");if(d.method==="PEINTURE"){const ids=new Set(paintForfaitIds(d.zone));return EXPERTISE_FORFAITS.filter(f=>ids.has(f.id));}return[];}
function lineHt(d:Damage){return forfaitById(d.forfaitId)?.ht??num(d.manualHt);}
function lineTtc(d:Damage){return forfaitById(d.forfaitId)?.ttc??lineHt(d)*1.2;}
function mechanicForfaits(kind:"service"|"tyre"|"brake",category:RecordData["mechanics"]["category"]){if(kind==="tyre")return EXPERTISE_FORFAITS.filter(f=>f.section==="MECANIQUE"&&/Montage et équilibrage|géométrie/i.test(f.label));if(!category)return[];const marker=`CATÉGORIE ${category}`;return EXPERTISE_FORFAITS.filter(f=>f.section==="MECANIQUE"&&f.category?.startsWith(marker)&&(kind==="service"?/Révision simple|Révision intermédiaire|Révision complète/i.test(f.label):/plaquettes|disques & plaquettes/i.test(f.label)));}
function gradeGuidance(grade:GradeKey|null,d:Damage){if(!grade)return{warn:true,text:"Définissez le grade avant validation."};const f=familyForZone(d.zone);if(grade==="ECO"&&!["wheel","glass","interior"].includes(f)&&["DSP","PEINTURE","PLUME","LUSTRAGE"].includes(d.method))return{warn:true,text:"Grade Éco : pas de remise en état carrosserie ; ne chiffrer que ce qui peut bloquer la vente."};if((grade==="ECO"||grade==="BRONZE")&&d.method==="JANTE")return{warn:true,text:`${GRADE_RULES[grade].label} : réparation jante non admise par le référentiel.`};if(d.method==="LUSTRAGE")return{warn:false,text:"Lustrage utilisé pour objectiver le défaut ; si facturé, renseigner le montant hors forfait."};if(d.method==="PARE_BRISE")return{warn:false,text:"Contrôler impact < 2 cm / fissure < 6 cm et les critères de remplacement du référentiel."};return{warn:false,text:`Décision contrôlée selon ${GRADE_RULES[grade].label}.`};}
function newDamage(zone=""):Damage{return{id:crypto.randomUUID(),zone,zoneLevel:"",type:"",description:"",sizeCm:"",method:familyForZone(zone)==="wheel"?"JANTE":familyForZone(zone)==="glass"?"PARE_BRISE":"",paintFinish:"",forfaitId:"",manualLabel:"",manualHt:"",photos:[]};}
async function imageData(file:File){if(!file.type.startsWith("image/"))return await fileToDataUrl(file);return new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(reader.error);reader.onload=()=>{const image=new Image();image.onload=()=>{const max=1440,scale=Math.min(1,max/Math.max(image.width,image.height)),w=Math.max(1,Math.round(image.width*scale)),h=Math.max(1,Math.round(image.height*scale)),canvas=document.createElement("canvas");canvas.width=w;canvas.height=h;const ctx=canvas.getContext("2d");if(!ctx)return reject(new Error("Canvas indisponible"));ctx.drawImage(image,0,0,w,h);resolve(canvas.toDataURL("image/jpeg",.76));};image.onerror=()=>reject(new Error("Image illisible"));image.src=String(reader.result);};reader.readAsDataURL(file);});}
function fileToDataUrl(file:File){return new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=()=>reject(reader.error);reader.readAsDataURL(file);});}
async function mediaRefs(files:FileList|null,photosOnly=false){if(!files?.length)return[] as MediaRef[];const result:MediaRef[]=[];for(const file of Array.from(files).slice(0,8)){if(photosOnly&&!file.type.startsWith("image/"))continue;if(!photosOnly&&!file.type.startsWith("image/")&&file.type!=="application/pdf")continue;if(file.size>3_500_000&&!file.type.startsWith("image/"))continue;try{result.push({id:crypto.randomUUID(),name:file.name,type:file.type,dataUrl:await imageData(file),createdAt:new Date().toISOString()});}catch{}}return result;}
function eventLabel(type:string){const labels:Record<string,string>={expert_validated:"Dossier validé par l'expert",client_submitted:"Dossier soumis au client",client_opened:"Réception prouvée · dossier ouvert",client_accepted:"Interventions acceptées",client_partially_accepted:"Acceptation partielle",client_refused:"Interventions refusées",message_expert:"Message envoyé au client",message_client:"Question / message du client"};return labels[type]??type.replaceAll("_"," ");}
function statusLabel(status?:string){const labels:Record<string,string>={draft:"Brouillon",validated:"Validé",submitted:"Soumis au client",viewed:"Ouvert par le client",accepted:"Accepté",partially_accepted:"Accepté partiellement",refused:"Refusé",closed:"Clôturé"};return labels[status??""]??"Brouillon";}

export default function ExpertiseWorkspace({mode="desktop"}:{mode?:Mode}){
  const[production,setProduction]=useState<ProductionPayload|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[query,setQuery]=useState("");const[queueMode,setQueueMode]=useState<"expertise"|"all">("expertise");const[selectedKey,setSelectedKey]=useState<string|null>(null);const[record,setRecord]=useState<RecordData|null>(null);const[workflow,setWorkflow]=useState<WorkflowPayload|null>(null);const[workflowLoading,setWorkflowLoading]=useState(false);const[dirty,setDirty]=useState(false);const[saving,setSaving]=useState(false);const[step,setStep]=useState<Step>("general");const[displayTax,setDisplayTax]=useState<"HT"|"TTC">("HT");const[chatDraft,setChatDraft]=useState("");const[rotation,setRotation]=useState({yaw:-28,pitch:-11});const drag=useRef<{x:number;y:number;yaw:number;pitch:number}|null>(null);
  async function loadProduction(){setLoading(true);try{const r=await fetch(`/api/development/production?fifo=0&_=${Date.now()}`,{cache:"no-store"});const payload=await r.json() as ProductionPayload;if(!r.ok)throw new Error(payload.error||`HTTP ${r.status}`);setProduction(payload);setError("");}catch(e){setError(e instanceof Error?e.message:"Reflet FTP indisponible");}finally{setLoading(false);}}
  useEffect(()=>{void loadProduction();const timer=window.setInterval(()=>void loadProduction(),60000);return()=>window.clearInterval(timer);},[]);
  const vehicles=production?.vehicles??[];const queue=useMemo(()=>vehicles.filter(v=>v.inFactory&&v.processProfile!=="EXCLU").filter(v=>queueMode==="all"||/réception|reception|expert|lavage|chiffr|devis|validation/i.test(v.status)).filter(v=>!query.trim()||`${v.registration??""} ${v.workOrder??""} ${v.vin??""} ${v.client??""} ${v.model??""} ${v.folderNumber??""}`.toLowerCase().includes(query.trim().toLowerCase())).sort((a,b)=>b.factoryAgeDays-a.factoryAgeDays),[vehicles,queueMode,query]);
  const selected=useMemo(()=>vehicles.find(v=>vehicleKey(v)===selectedKey)??null,[vehicles,selectedKey]);

  async function refreshWorkflow(key:string,hydrate=false){try{const r=await fetch(`/api/development/expertise/workflow?vehicleKey=${encodeURIComponent(key)}&_=${Date.now()}`,{cache:"no-store"});const payload=await r.json() as WorkflowPayload;if(!r.ok)throw new Error(payload.error||`HTTP ${r.status}`);setWorkflow(payload);if(hydrate){const saved=payload.draftSnapshot?.record;setRecord(normalizeRecord(saved,key));setDirty(false);}}catch(e){if(hydrate)setRecord(emptyRecord(key));setError(e instanceof Error?e.message:"Workflow expertise indisponible");}}
  async function chooseVehicle(v:Vehicle){const key=vehicleKey(v);setSelectedKey(key);setStep("general");setWorkflow(null);setRecord(null);setWorkflowLoading(true);await refreshWorkflow(key,true);setWorkflowLoading(false);if(mode==="mobile")window.scrollTo({top:0});}
  useEffect(()=>{if(selectedKey)return;const first=queue[0];if(first)void chooseVehicle(first);},[queue,selectedKey]);
  useEffect(()=>{if(!selectedKey)return;const timer=window.setInterval(()=>void refreshWorkflow(selectedKey,false),15000);return()=>window.clearInterval(timer);},[selectedKey]);
  function mutate(fn:(current:RecordData)=>RecordData){setRecord(current=>{if(!current)return current;setDirty(true);return{...fn(current),updatedAt:new Date().toISOString()};});}

  const quoteItems=useMemo<QuoteItem[]>(()=>{if(!record)return[];const items:QuoteItem[]=[];const add=(item:Omit<QuoteItem,"clientSelectable">)=>items.push({...item,clientSelectable:Boolean(record.clientOptions[item.key])});const mech=record.mechanics;for(const[key,category,forfaitId,defect]of [["service","Entretien",mech.serviceForfaitId,mech.serviceDue==="OUI"?"Entretien requis sous 6 mois / 10 000 km":"Entretien sélectionné"],["tyres","Pneumatiques",mech.tyreForfaitId,mech.tyres.filter(t=>t.status==="REMPLACER").map(t=>`${t.position} ${t.brand} ${t.dimension} ${t.treadMm} mm`).join(" · ")],["brake-front","Freinage",mech.brakeFrontForfaitId,`Avant : ${mech.brakeFront||"à contrôler"}`],["brake-rear","Freinage",mech.brakeRearForfaitId,`Arrière : ${mech.brakeRear||"à contrôler"}`]]as const){const f=forfaitById(forfaitId);if(f)add({key,category,label:f.label,defect,justification:mech.notes,method:"Forfait mécanique",photoData:null,amountHt:f.ht,amountTtc:f.ttc});}const shock=num(mech.shockManualHt);if(shock>0)add({key:"shocks",category:"Amortisseurs",label:"Remise en état amortisseurs",defect:`AV ${mech.shockFront||"—"} · AR ${mech.shockRear||"—"}`,justification:mech.notes,method:"Chiffrage expert",photoData:null,amountHt:shock,amountTtc:shock*1.2});record.damages.forEach((d,index)=>{const ht=lineHt(d),ttc=lineTtc(d),f=forfaitById(d.forfaitId),label=f?.label||d.manualLabel||`${d.zone} · ${METHOD_LABELS[d.method]||d.method||"Décision à définir"}`;if(ht>0||d.method==="SURVEILLANCE")add({key:`damage:${d.id}`,category:"Carrosserie",label,defect:`${d.zone||"Élément"} · ${d.type||"défaut à préciser"}${d.sizeCm?` · ${d.sizeCm} cm`:""}`,justification:d.description,method:METHOD_LABELS[d.method]||d.method,photoData:d.photos[0]?.dataUrl??null,amountHt:ht,amountTtc:ttc});});return items;},[record]);
  const totals=useMemo(()=>quoteItems.reduce((sum,item)=>({ht:sum.ht+item.amountHt,ttc:sum.ttc+item.amountTtc}),{ht:0,ttc:0}),[quoteItems]);
  const blockers=useMemo(()=>{if(!record||!selected)return["Sélectionnez un dossier."];const list:string[]=[];if(!record.grade)list.push("Grade véhicule à définir.");if(!selected.workOrder&&!selected.vin)list.push("OR ou VIN absent du dossier source.");if(record.mechanics.serviceDue==="OUI"&&!record.mechanics.serviceForfaitId)list.push("Entretien requis : forfait à choisir.");record.damages.forEach((d,index)=>{if(!d.zone||!d.type||!d.method)list.push(`Dommage ${index+1} incomplet (élément / type / méthode).`);if(!["SURVEILLANCE"].includes(d.method)&&lineHt(d)<=0)list.push(`Dommage ${index+1} : chiffrage manquant.`);if(lineHt(d)>0&&!d.photos.length)list.push(`Dommage ${index+1} : photo justificative obligatoire.`);if(lineHt(d)>0&&!d.description.trim())list.push(`Dommage ${index+1} : justification expert obligatoire.`);});return list;},[record,selected]);

  function snapshot(){return{version:2,record,vehicle:selected,totals,expertConclusion:record?.general.generalComment??"",generatedAt:new Date().toISOString()};}
  async function saveWorkflow(action:"draft"|"validate"|"submit"){if(!record||!selected)return;setSaving(true);setError("");try{const r=await fetch("/api/development/expertise/workflow",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,vehicle:{vehicleKey:vehicleKey(selected),registration:selected.registration,workOrder:selected.workOrder,vin:selected.vin,client:selected.client,model:selected.model,mileage:selected.mileage},snapshot:snapshot(),items:quoteItems})});const payload=await r.json() as WorkflowPayload;if(!r.ok)throw new Error(payload.error||"Enregistrement impossible");setWorkflow(payload);setDirty(false);}catch(e){setError(e instanceof Error?e.message:"Enregistrement impossible");}finally{setSaving(false);}}
  useEffect(()=>{if(!dirty||!record||!selected||saving)return;const timer=window.setTimeout(()=>void saveWorkflow("draft"),1300);return()=>window.clearTimeout(timer);},[dirty,record,selected,saving]);
  async function sendMessage(){if(!selectedKey||!chatDraft.trim())return;const body=chatDraft.trim();setChatDraft("");try{const r=await fetch("/api/development/expertise/workflow",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"message",vehicleKey:selectedKey,message:body})});const payload=await r.json() as WorkflowPayload;if(!r.ok)throw new Error(payload.error||"Message non envoyé");setWorkflow(payload);}catch(e){setError(e instanceof Error?e.message:"Message non envoyé");}}
  function addDamage(zone=""){mutate(r=>({...r,damages:[...r.damages,newDamage(zone)]}));setStep("body");}
  function updateDamage(id:string,patch:Partial<Damage>){mutate(r=>({...r,damages:r.damages.map(d=>{if(d.id!==id)return d;const zoneChanged=patch.zone!==undefined&&patch.zone!==d.zone,methodChanged=patch.method!==undefined&&patch.method!==d.method;return{...d,...patch,...(zoneChanged?{type:"",method:familyForZone(String(patch.zone||""))==="wheel"?"JANTE":familyForZone(String(patch.zone||""))==="glass"?"PARE_BRISE":"",forfaitId:"",paintFinish:""}:{}),...(methodChanged?{forfaitId:""}:{})};})}));}
  async function addGeneralMedia(bucket:keyof RecordData["general"],files:FileList|null,photosOnly=false){if(!record)return;const refs=await mediaRefs(files,photosOnly);if(!refs.length)return;mutate(r=>({...r,general:{...r.general,[bucket]:[...(r.general[bucket] as MediaRef[]),...refs]}}));}
  async function addDamageMedia(id:string,files:FileList|null){const refs=await mediaRefs(files,true);if(!refs.length)return;mutate(r=>({...r,damages:r.damages.map(d=>d.id===id?{...d,photos:[...d.photos,...refs]}:d)}));}
  function updateTyre(position:Tyre["position"],patch:Partial<Tyre>){mutate(r=>({...r,mechanics:{...r.mechanics,tyres:r.mechanics.tyres.map(t=>t.position===position?{...t,...patch}:t)}}));}
  const serviceOptions=record?mechanicForfaits("service",record.mechanics.category):[],tyreOptions=record?mechanicForfaits("tyre",record.mechanics.category):[],brakeOptions=record?mechanicForfaits("brake",record.mechanics.category):[];
  const caseData=workflow?.case??null,shareUrl=caseData?.shareToken&&typeof window!=="undefined"?`${window.location.origin}/expertise/client/${caseData.shareToken}`:"";
  const completedSteps=new Set<Step>();if(record?.grade)completedSteps.add("general");if(record&&(record.mechanics.serviceDue||record.mechanics.tyres.some(t=>t.status)))completedSteps.add("mechanics");if(record&&record.damages.every(d=>d.zone&&d.type&&d.method))completedSteps.add("body");if(caseData?.validatedAt)completedSteps.add("quote");

  const queuePane=<aside className={styles.queue}><div className={styles.queueHead}><div><span className={styles.eyebrow}>FILE EXPERTISE</span><h2>Dossiers à contrôler</h2></div><small>{queue.length} dossiers</small></div><input className={styles.search} value={query} onChange={e=>setQuery(e.target.value)} placeholder="Immat, OR, VIN, client, modèle…"/><div className={styles.queueFilters}><button className={queueMode==="expertise"?styles.active:""} onClick={()=>setQueueMode("expertise")}>EXPERTISE / CHIFFRAGE</button><button className={queueMode==="all"?styles.active:""} onClick={()=>setQueueMode("all")}>TOUT LE PARC</button></div><div className={styles.vehicleList}>{queue.slice(0,500).map(v=>{const key=vehicleKey(v);return <button key={key} className={`${styles.vehicle} ${selectedKey===key?styles.vehicleActive:""}`} onClick={()=>void chooseVehicle(v)}><div><strong>{v.registration||"Sans immat"}</strong><span>OR {v.workOrder||"—"} · {v.model||"—"}</span><small>{v.status} · {v.location||"position —"} · J+{Math.round(v.factoryAgeDays)}</small></div>{selectedKey===key&&caseData&&<b className={styles.badge}>{statusLabel(caseData.status)}</b>}</button>;})}</div></aside>;

  return <main className={`${styles.page} ${mode==="mobile"?styles.mobile:""}`}>
    <header className={styles.header}><div className={styles.brand}><img src="/crvo-logo.png" alt="CRVO"/><div><span>{mode==="mobile"?"PWA EXPERTISE TERRAIN":"SAS EXPERTISE · WORKFLOW CLIENT"}</span><h1>Expertise & devis</h1><p>Défaut → preuve photo → méthode → forfait → décision client → traçabilité.</p></div></div><div className={styles.headerActions}>{mode==="mobile"&&selected&&<button className={`${styles.ghost} ${styles.mobileBack}`} onClick={()=>{setSelectedKey(null);setRecord(null);setWorkflow(null);}}>← DOSSIERS</button>}<button className={styles.ghost} onClick={()=>void loadProduction()} disabled={loading}>{loading?"ACTUALISATION…":"ACTUALISER FTP"}</button>{mode==="desktop"&&<a className={styles.button} href="/developpement">SAS DÉVELOPPEMENT</a>}</div></header>
    <section className={styles.notice}><strong>DEV SANDBOX</strong><span>Dossiers FTP réels. Le workflow expertise, le rapport client et les décisions sont persistés dans KPI CRVO ; aucune écriture MecaPlanning Factory / Fleetback.</span></section>{error&&<div className={styles.error}>{error}</div>}
    <div className={styles.layout}>{(mode==="desktop"||!selected)&&queuePane}<section className={styles.workspace}>{!selected||!record||workflowLoading?<div className={styles.empty}><div><strong>{workflowLoading?"Chargement du dossier…":"Sélectionnez un dossier"}</strong><span>Le dossier reprend l'identité FTP/MPF puis guide l'expert jusqu'à la décision client.</span></div></div>:<>
      <div className={styles.vehicleHeader}><div><span className={styles.eyebrow}>DOSSIER · {statusLabel(caseData?.status)}</span><h2>{selected.registration||"Sans immatriculation"}</h2><p>{selected.client||"Client non renseigné"} · {[selected.manufacturer,selected.model].filter(Boolean).join(" ")||"Véhicule non renseigné"}</p><div className={styles.vehicleMeta}><span>OR {selected.workOrder||"—"}</span><span>Dossier {selected.folderNumber||"—"}</span><span>VIN {selected.vin||"—"}</span><span>{Math.round(selected.mileage||0).toLocaleString("fr-FR")} km</span><span>Position {selected.location||"—"}</span><span>{selected.status}</span></div></div><div className={styles.totals}><span>DEVIS ACTUEL</span><strong>{money(displayTax==="HT"?totals.ht:totals.ttc)} {displayTax}</strong><b>{quoteItems.length} intervention{quoteItems.length>1?"s":""}</b></div></div>
      <div className={styles.stepRail}>{STEPS.map(s=><button key={s.key} className={step===s.key?styles.stepActive:completedSteps.has(s.key)?styles.stepDone:""} onClick={()=>setStep(s.key)}>{completedSteps.has(s.key)&&step!==s.key?"✓ ":""}{s.label}</button>)}</div>

      {step==="general"&&<article className={styles.panel}><div className={styles.sectionHead}><div><span className={styles.eyebrow}>01 · DOSSIER & ASPECT GÉNÉRAL</span><h2>Comprendre le véhicule avant de chiffrer</h2><p>Le client doit pouvoir relier chaque coût à un constat objectif.</p></div><small>Autosauvegarde serveur · {dt(record.updatedAt)}</small></div><div className={styles.grid2}><label className={styles.field}><span>Grade du véhicule *</span><select className={styles.select} value={record.grade||""} onChange={e=>mutate(r=>({...r,grade:(e.target.value||null)as GradeKey|null}))}><option value="">À définir</option>{(Object.keys(GRADE_RULES)as GradeKey[]).map(k=><option key={k} value={k}>{GRADE_RULES[k].label}</option>)}</select></label><label className={styles.field}><span>Historique d'entretien</span><select className={styles.select} value={record.general.maintenanceHistory} onChange={e=>mutate(r=>({...r,general:{...r.general,maintenanceHistory:e.target.value as RecordData["general"]["maintenanceHistory"]}}))}><option value="">À contrôler</option><option>OUI</option><option>NON</option><option>INCONNU</option></select></label><label className={`${styles.field} ${styles.span2}`}><span>Historique / contexte</span><textarea className={styles.textarea} value={record.general.historyComment} onChange={e=>mutate(r=>({...r,general:{...r.general,historyComment:e.target.value}}))} placeholder="Carnet, factures, réserves, contexte du véhicule…"/></label></div>{record.grade&&<div className={styles.gradeStrip} style={{borderLeftColor:GRADE_RULES[record.grade].accent}}><strong>{GRADE_RULES[record.grade].label}</strong><span>{GRADE_RULES[record.grade].summary}</span></div>}<div className={styles.mediaGrid}><MediaBox title="Photos extérieur" subtitle="4 faces + état général" refs={record.general.exteriorPhotos} onAdd={f=>void addGeneralMedia("exteriorPhotos",f,true)} onDelete={id=>mutate(r=>({...r,general:{...r.general,exteriorPhotos:r.general.exteriorPhotos.filter(x=>x.id!==id)}}))}/><MediaBox title="Photos intérieur" subtitle="Habitacle, sellerie, coffre" refs={record.general.interiorPhotos} onAdd={f=>void addGeneralMedia("interiorPhotos",f,true)} onDelete={id=>mutate(r=>({...r,general:{...r.general,interiorPhotos:r.general.interiorPhotos.filter(x=>x.id!==id)}}))}/><MediaBox title="Documents entretien" subtitle="PDF ou photo" refs={record.general.maintenanceDocuments} onAdd={f=>void addGeneralMedia("maintenanceDocuments",f)} onDelete={id=>mutate(r=>({...r,general:{...r.general,maintenanceDocuments:r.general.maintenanceDocuments.filter(x=>x.id!==id)}}))}/><MediaBox title="Autres documents" subtitle="Éléments utiles au dossier" refs={record.general.otherDocuments} onAdd={f=>void addGeneralMedia("otherDocuments",f)} onDelete={id=>mutate(r=>({...r,general:{...r.general,otherDocuments:r.general.otherDocuments.filter(x=>x.id!==id)}}))}/></div><div className={styles.actionRow} style={{marginTop:12,justifyContent:"flex-end"}}><button className={styles.button} onClick={()=>setStep("mechanics")}>CONTINUER · MÉCANIQUE →</button></div></article>}

      {step==="mechanics"&&<article className={styles.panel}><div className={styles.sectionHead}><div><span className={styles.eyebrow}>02 · MÉCANIQUE</span><h2>Entretien, pneus, freins & amortisseurs</h2><p>Le forfait proposé dépend du besoin réellement constaté.</p></div></div><div className={styles.grid3}><label className={styles.field}><span>Catégorie tarifaire</span><select className={styles.select} value={record.mechanics.category} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,category:e.target.value as RecordData["mechanics"]["category"],serviceForfaitId:"",brakeFrontForfaitId:"",brakeRearForfaitId:""}}))}><option value="">À définir</option><option value="A">Catégorie A</option><option value="B">Catégorie B</option><option value="C">Catégorie C</option></select></label><label className={styles.field}><span>Entretien requis sous 6 mois / 10 000 km</span><select className={styles.select} value={record.mechanics.serviceDue} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,serviceDue:e.target.value as RecordData["mechanics"]["serviceDue"]}}))}><option value="">À contrôler</option><option>OUI</option><option>NON</option></select></label><label className={styles.field}><span>Forfait entretien</span><select className={styles.select} value={record.mechanics.serviceForfaitId} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,serviceForfaitId:e.target.value}}))}><option value="">Aucun</option>{serviceOptions.map(f=><option key={f.id} value={f.id}>{f.label} · {money(f.ht)} HT</option>)}</select></label></div><div className={styles.tyreGrid}>{record.mechanics.tyres.map(t=><article className={styles.tyre} key={t.position}><strong>{t.position}</strong><label className={styles.field}><span>Marque</span><input className={styles.input} value={t.brand} onChange={e=>updateTyre(t.position,{brand:e.target.value})}/></label><label className={styles.field}><span>Dimension</span><input className={styles.input} value={t.dimension} onChange={e=>updateTyre(t.position,{dimension:e.target.value})}/></label><label className={styles.field}><span>Usure mm</span><input className={styles.input} inputMode="decimal" value={t.treadMm} onChange={e=>updateTyre(t.position,{treadMm:e.target.value})}/></label><label className={styles.field}><span>Décision</span><select className={styles.select} value={t.status} onChange={e=>updateTyre(t.position,{status:e.target.value as Tyre["status"]})}><option value="">À contrôler</option><option>OK</option><option>SURVEILLER</option><option>REMPLACER</option></select></label><label className={styles.switch}><input type="checkbox" checked={t.runflat} onChange={e=>updateTyre(t.position,{runflat:e.target.checked})}/> Runflat</label></article>)}</div><div className={styles.grid3}><label className={styles.field}><span>Forfait pneus / géométrie</span><select className={styles.select} value={record.mechanics.tyreForfaitId} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,tyreForfaitId:e.target.value}}))}><option value="">Aucun</option>{tyreOptions.map(f=><option key={f.id} value={f.id}>{f.label} · {money(f.ht)} HT</option>)}</select></label><label className={styles.field}><span>Frein AV</span><select className={styles.select} value={record.mechanics.brakeFront} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,brakeFront:e.target.value as RecordData["mechanics"]["brakeFront"]}}))}><option value="">À contrôler</option><option>OK</option><option value="PLAQUETTES">Plaquettes</option><option value="DISQUES_PLAQUETTES">Disques + plaquettes</option></select></label><label className={styles.field}><span>Forfait frein AV</span><select className={styles.select} value={record.mechanics.brakeFrontForfaitId} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,brakeFrontForfaitId:e.target.value}}))}><option value="">Aucun</option>{brakeOptions.filter(f=>/AVANT/i.test(f.label)).map(f=><option key={f.id} value={f.id}>{f.label} · {money(f.ht)} HT</option>)}</select></label><label className={styles.field}><span>Frein AR</span><select className={styles.select} value={record.mechanics.brakeRear} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,brakeRear:e.target.value as RecordData["mechanics"]["brakeRear"]}}))}><option value="">À contrôler</option><option>OK</option><option value="PLAQUETTES">Plaquettes</option><option value="DISQUES_PLAQUETTES">Disques + plaquettes</option></select></label><label className={styles.field}><span>Forfait frein AR</span><select className={styles.select} value={record.mechanics.brakeRearForfaitId} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,brakeRearForfaitId:e.target.value}}))}><option value="">Aucun</option>{brakeOptions.filter(f=>/ARRIERE|ARRIÈRE/i.test(f.label)).map(f=><option key={f.id} value={f.id}>{f.label} · {money(f.ht)} HT</option>)}</select></label><label className={styles.field}><span>Amortisseurs AV</span><select className={styles.select} value={record.mechanics.shockFront} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,shockFront:e.target.value as RecordData["mechanics"]["shockFront"]}}))}><option value="">À contrôler</option><option>OK</option><option>FUITE</option><option>JEU</option><option>REMPLACER</option></select></label><label className={styles.field}><span>Amortisseurs AR</span><select className={styles.select} value={record.mechanics.shockRear} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,shockRear:e.target.value as RecordData["mechanics"]["shockRear"]}}))}><option value="">À contrôler</option><option>OK</option><option>FUITE</option><option>JEU</option><option>REMPLACER</option></select></label><label className={styles.field}><span>Chiffrage amortisseurs HT</span><input className={styles.input} inputMode="decimal" value={record.mechanics.shockManualHt} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,shockManualHt:e.target.value}}))}/></label><label className={`${styles.field} ${styles.span2}`}><span>Commentaire / justification mécanique</span><textarea className={styles.textarea} value={record.mechanics.notes} onChange={e=>mutate(r=>({...r,mechanics:{...r.mechanics,notes:e.target.value}}))} placeholder="Constats, mesures, usure, nécessité de remplacement…"/></label></div><div className={styles.actionRow} style={{marginTop:12,justifyContent:"space-between"}}><button className={styles.ghost} onClick={()=>setStep("general")}>← DOSSIER</button><button className={styles.button} onClick={()=>setStep("body")}>CONTINUER · CARROSSERIE →</button></div></article>}

      {step==="body"&&<article className={styles.panel}><div className={styles.sectionHead}><div><span className={styles.eyebrow}>03 · CARROSSERIE</span><h2>Vue éclatée 360° & dommages</h2><p>Un coût carrosserie doit toujours être rattaché à un défaut, une photo et une justification.</p></div><button className={styles.secondary} onClick={()=>addDamage("")}>+ DOMMAGE LIBRE</button></div><div className={styles.bodyLayout}><ExplodedVehicle rotation={rotation} setRotation={setRotation} drag={drag} damaged={record.damages.map(d=>d.zone)} onSelect={addDamage}/><div className={styles.damageList}>{!record.damages.length?<div className={styles.empty} style={{minHeight:260}}><div><strong>Aucun dommage déclaré</strong><span>Cliquez sur un élément du véhicule ou ajoutez un dommage libre.</span></div></div>:record.damages.map((d,index)=>{const forfaits=forfaitsForDamage(d),guide=gradeGuidance(record.grade,d),forfait=forfaitById(d.forfaitId);return <article className={styles.damage} key={d.id}><div className={styles.damageHead}><div><span className={styles.eyebrow}>DOMMAGE #{index+1}</span><strong>{d.zone||"Élément à définir"}</strong></div><button className={styles.danger} onClick={()=>mutate(r=>({...r,damages:r.damages.filter(x=>x.id!==d.id)}))}>SUPPRIMER</button></div><div className={styles.grid2}><label className={styles.field}><span>Élément *</span><select className={styles.select} value={d.zone} onChange={e=>updateDamage(d.id,{zone:e.target.value})}><option value="">Sélectionner</option>{BODY_ZONES.map(z=><option key={z}>{z}</option>)}</select></label><label className={styles.field}><span>Type de dommage *</span><select className={styles.select} value={d.type} onChange={e=>updateDamage(d.id,{type:e.target.value})}><option value="">Sélectionner</option>{damageTypesForZone(d.zone).map(x=><option key={x}>{x}</option>)}</select></label>{!["wheel","glass","interior"].includes(familyForZone(d.zone))&&<label className={styles.field}><span>Zone grade</span><select className={styles.select} value={d.zoneLevel} onChange={e=>updateDamage(d.id,{zoneLevel:e.target.value as Damage["zoneLevel"]})}><option value="">À préciser</option><option value="HAUTE">Zone haute</option><option value="BASSE">Zone basse</option></select></label>}<label className={styles.field}><span>Taille / longueur cm</span><input className={styles.input} inputMode="decimal" value={d.sizeCm} onChange={e=>updateDamage(d.id,{sizeCm:e.target.value})}/></label><label className={styles.field}><span>Méthode *</span><select className={styles.select} value={d.method} onChange={e=>updateDamage(d.id,{method:e.target.value})}><option value="">Sélectionner</option>{methodsForZone(d.zone).map(x=><option key={x} value={x}>{METHOD_LABELS[x]||x}</option>)}</select></label><label className={styles.field}><span>Forfait compatible</span><select className={styles.select} value={d.forfaitId} onChange={e=>updateDamage(d.id,{forfaitId:e.target.value})} disabled={!forfaits.length}><option value="">{forfaits.length?"Sélectionner":"Hors forfait / aucun tarif automatique"}</option>{forfaits.map(f=><option key={f.id} value={f.id}>{f.label} · {money(f.ht)} HT</option>)}</select></label>{!forfait&&d.method!=="SURVEILLANCE"&&<><label className={styles.field}><span>Libellé hors forfait</span><input className={styles.input} value={d.manualLabel} onChange={e=>updateDamage(d.id,{manualLabel:e.target.value})}/></label><label className={styles.field}><span>Montant HT</span><input className={styles.input} inputMode="decimal" value={d.manualHt} onChange={e=>updateDamage(d.id,{manualHt:e.target.value})}/></label></>}<label className={`${styles.field} ${styles.span2}`}><span>Pourquoi cette intervention ? *</span><textarea className={styles.textarea} value={d.description} onChange={e=>updateDamage(d.id,{description:e.target.value})} placeholder="Décrire le défaut visible et expliquer pourquoi la méthode / le coût sont justifiés…"/></label></div><div className={`${styles.guidance} ${guide.warn?styles.guidanceWarn:""}`}><strong>Contrôle métier : </strong>{guide.text}</div><MediaBox title="Photos du défaut" subtitle="Preuve visuelle transmise au client" refs={d.photos} onAdd={f=>void addDamageMedia(d.id,f)} onDelete={id=>mutate(r=>({...r,damages:r.damages.map(x=>x.id===d.id?{...x,photos:x.photos.filter(p=>p.id!==id)}:x)}))}/><div className={styles.priceLine}><span>{forfait?.label||d.manualLabel||METHOD_LABELS[d.method]||"À chiffrer"}</span><strong>{money(lineHt(d))} HT · {money(lineTtc(d))} TTC</strong></div></article>;})}</div></div><div className={styles.actionRow} style={{marginTop:12,justifyContent:"space-between"}}><button className={styles.ghost} onClick={()=>setStep("mechanics")}>← MÉCANIQUE</button><button className={styles.button} onClick={()=>setStep("quote")}>CONTINUER · DEVIS →</button></div></article>}

      {step==="quote"&&<article className={styles.panel}><div className={styles.sectionHead}><div><span className={styles.eyebrow}>04 · DEVIS & PARCOURS CLIENT</span><h2>Valider, soumettre, expliquer, tracer</h2><p>L'expert décide quelles lignes sont négociables. Les autres restent obligatoires dans l'acceptation.</p></div><div className={styles.statusActions}><span className={`${styles.statusChip} ${caseData?.status==="validated"?styles.statusValidated:["submitted","viewed"].includes(caseData?.status??"")?styles.statusSubmitted:["accepted","partially_accepted","refused"].includes(caseData?.status??"")?styles.statusDecision:""}`}>{statusLabel(caseData?.status)}</span><button className={styles.ghost} onClick={()=>setDisplayTax(displayTax==="HT"?"TTC":"HT")}>AFFICHER {displayTax==="HT"?"TTC":"HT"}</button></div></div><div className={styles.summary}><article><span>Interventions</span><strong>{quoteItems.length}</strong></article><article><span>Optionnelles</span><strong>{quoteItems.filter(i=>i.clientSelectable).length}</strong></article><article><span>Total HT</span><strong>{money(totals.ht)}</strong></article><article><span>Total TTC</span><strong>{money(totals.ttc)}</strong></article><article className={styles.summaryTotal}><span>Total affiché</span><strong>{money(displayTax==="HT"?totals.ht:totals.ttc)} {displayTax}</strong></article></div><div className={styles.quoteLines}>{quoteItems.map(item=><div className={styles.quoteLine} key={item.key}><div><strong>{item.label}</strong><small>{item.defect}{item.justification?` · ${item.justification}`:""}</small></div><div className={styles.quoteLinePrice}>{money(displayTax==="HT"?item.amountHt:item.amountTtc)} {displayTax}</div><label className={styles.switch}><input type="checkbox" checked={Boolean(record.clientOptions[item.key])} onChange={e=>mutate(r=>({...r,clientOptions:{...r.clientOptions,[item.key]:e.target.checked}}))}/> Client peut choisir</label></div>)}</div>{!quoteItems.length&&<div className={styles.gradeStrip}><strong>Aucune intervention facturée</strong><span>L'expertise peut être validée à 0 €, avec une conclusion claire.</span></div>}<label className={styles.field} style={{marginTop:12}}><span>Conclusion générale adressée au client</span><textarea className={styles.textarea} value={record.general.generalComment} onChange={e=>mutate(r=>({...r,general:{...r.general,generalComment:e.target.value}}))} placeholder="Synthèse courte : état du véhicule, travaux nécessaires, éléments optionnels et éventuelles réserves…"/></label>{blockers.length>0&&<div className={styles.error} style={{margin:"12px 0 0"}}><strong>Avant validation :</strong><ul>{blockers.map(b=><li key={b}>{b}</li>)}</ul></div>}<div className={styles.actionRow} style={{marginTop:12,justifyContent:"space-between",flexWrap:"wrap"}}><button className={styles.ghost} onClick={()=>setStep("body")}>← CARROSSERIE</button><div className={styles.actionRow}><button className={styles.success} disabled={saving||blockers.length>0} onClick={()=>void saveWorkflow("validate")}>{saving?"ENREGISTREMENT…":"VALIDER LE DOSSIER"}</button><button className={styles.button} disabled={saving||blockers.length>0} onClick={()=>void saveWorkflow("submit")}>{saving?"ENREGISTREMENT…":"SOUMETTRE AU CLIENT"}</button></div></div>{shareUrl&&<div className={styles.share}><strong>LIEN CLIENT · RÉVISION {caseData?.currentRevision}</strong><code>{shareUrl}</code><div className={styles.shareActions}><button className={styles.ghost} onClick={()=>void navigator.clipboard.writeText(shareUrl)}>COPIER LE LIEN</button><a className={styles.button} href={shareUrl} target="_blank" rel="noreferrer">OUVRIR VUE CLIENT</a></div></div>}<div className={styles.workflowGrid}><div className={styles.timeline}><h3>Traçabilité du dossier</h3>{(workflow?.events??[]).slice().reverse().map(e=><div className={styles.event} key={e.id}><strong>{eventLabel(e.type)}</strong><span>{e.actorName||e.actorRole} · {dt(e.createdAt)}</span><small>{typeof e.details?.revision==="number"?`Révision ${e.details.revision}`:""}{typeof e.details?.approvedTtc==="number"?` · accepté ${money(Number(e.details.approvedTtc))} TTC`:""}</small></div>)}{!(workflow?.events?.length)&&<small>Aucun événement horodaté. La validation crée la première trace immuable.</small>}{caseData?.submittedAt&&<div className={styles.gradeStrip}><strong>Soumis : {dt(caseData.submittedAt)}</strong><span>{caseData.firstOpenedAt?`Réception prouvée / dossier ouvert : ${dt(caseData.firstOpenedAt)}`:"En attente de la première ouverture client."}</span>{caseData.clientDecidedAt&&<span>Décision : {dt(caseData.clientDecidedAt)}</span>}</div>}</div><div className={styles.chat}><h3>Chat expert ↔ client</h3><div className={styles.messageList}>{(workflow?.messages??[]).map(m=><div className={`${styles.message} ${m.authorRole==="client"?styles.messageClient:""}`} key={m.id}><strong>{m.authorRole==="client"?"Client":m.authorName||"Expert CRVO"}</strong><span>{m.body}</span><small>{dt(m.createdAt)}</small></div>)}{!(workflow?.messages?.length)&&<small>Aucun échange pour le moment.</small>}</div><div className={styles.chatForm}><input value={chatDraft} onChange={e=>setChatDraft(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"){e.preventDefault();void sendMessage();}}} placeholder={caseData?.submittedAt?"Répondre au client…":"Le chat sera utile après soumission…"}/><button className={styles.button} disabled={!caseData?.submittedAt||!chatDraft.trim()} onClick={()=>void sendMessage()}>ENVOYER</button></div></div></div></article>}
    </>}</section></div>
  </main>;
}

function MediaBox({title,subtitle,refs,onAdd,onDelete}:{title:string;subtitle:string;refs:MediaRef[];onAdd:(files:FileList|null)=>void;onDelete:(id:string)=>void}){return <div className={styles.mediaBox}><div className={styles.mediaTitle}><div><strong>{title}</strong><span>{subtitle}</span></div><b>{refs.length}</b></div><div className={styles.mediaThumbs}>{refs.map(ref=>ref.type.startsWith("image/")?<div className={styles.thumb} key={ref.id}><img src={ref.dataUrl} alt={ref.name}/><button onClick={()=>onDelete(ref.id)}>×</button></div>:<div className={styles.doc} key={ref.id}><a href={ref.dataUrl} target="_blank" rel="noreferrer"><span>{ref.name}</span></a><button onClick={()=>onDelete(ref.id)}>×</button></div>)}<label className={styles.mediaAdd}>+ AJOUTER<input type="file" accept="image/*,application/pdf" multiple onChange={e=>{onAdd(e.target.files);e.currentTarget.value="";}}/></label></div></div>}

function ExplodedVehicle({rotation,setRotation,drag,damaged,onSelect}:{rotation:{yaw:number;pitch:number};setRotation:React.Dispatch<React.SetStateAction<{yaw:number;pitch:number}>>;drag:React.MutableRefObject<{x:number;y:number;yaw:number;pitch:number}|null>;damaged:string[];onSelect:(zone:string)=>void}){const parts=[{z:"Capot",l:"CAPOT",c:styles.hood},{z:"Pavillon",l:"PAVILLON",c:styles.roof},{z:"Hayon / coffre",l:"HAYON",c:styles.tailgate},{z:"Bouclier AV",l:"BOUCLIER AV",c:styles.front},{z:"Bouclier AR",l:"BOUCLIER AR",c:styles.rear},{z:"Porte AVG",l:"PORTE AVG",c:styles.lfd},{z:"Porte ARG",l:"PORTE ARG",c:styles.lrd},{z:"Porte AVD",l:"PORTE AVD",c:styles.rfd},{z:"Porte ARD",l:"PORTE ARD",c:styles.rrd},{z:"Aile AVG",l:"AILE AVG",c:styles.lfw},{z:"Aile ARG",l:"AILE ARG",c:styles.lrw},{z:"Aile AVD",l:"AILE AVD",c:styles.rfw},{z:"Aile ARD",l:"AILE ARD",c:styles.rrw},{z:"Rétroviseur AVG",l:"RÉTRO G",c:styles.lmirror},{z:"Rétroviseur AVD",l:"RÉTRO D",c:styles.rmirror},{z:"Pare-brise",l:"PARE-BRISE",c:styles.windshield},{z:"Jante AVG",l:"AVG",c:`${styles.wheel} ${styles.wfl}`},{z:"Jante AVD",l:"AVD",c:`${styles.wheel} ${styles.wfr}`},{z:"Jante ARG",l:"ARG",c:`${styles.wheel} ${styles.wrl}`},{z:"Jante ARD",l:"ARD",c:`${styles.wheel} ${styles.wrr}`}];return <div className={styles.viewer}><div className={styles.viewerHead}><div><strong>CARROSSERIE ÉCLATÉE 3D</strong><span>Glisser horizontalement pour tourner · cliquer pour déclarer</span></div><button onClick={()=>setRotation({yaw:-28,pitch:-11})}>RECENTRER</button></div><div className={styles.stage} onPointerDown={e=>{if((e.target as HTMLElement).closest("button"))return;drag.current={x:e.clientX,y:e.clientY,yaw:rotation.yaw,pitch:rotation.pitch};e.currentTarget.setPointerCapture(e.pointerId);}} onPointerMove={e=>{if(!drag.current)return;setRotation({yaw:drag.current.yaw+(e.clientX-drag.current.x)*.5,pitch:Math.max(-32,Math.min(15,drag.current.pitch-(e.clientY-drag.current.y)*.18))});}} onPointerUp={()=>{drag.current=null;}} onPointerCancel={()=>{drag.current=null;}}><div className={styles.scene} style={{transform:`translate(-50%,-50%) rotateX(${rotation.pitch}deg) rotateY(${rotation.yaw}deg)`}}><div className={styles.chassis}/>{parts.map(p=><button key={p.z} className={`${styles.part} ${p.c} ${damaged.includes(p.z)?styles.damaged:""}`} onClick={()=>onSelect(p.z)}>{p.l}</button>)}</div></div><div className={styles.actionRow} style={{justifyContent:"center"}}><button className={styles.ghost} onClick={()=>setRotation(r=>({...r,yaw:r.yaw-45}))}>↶ 45°</button><button className={styles.ghost} onClick={()=>setRotation(r=>({...r,yaw:r.yaw+45}))}>45° ↷</button></div></div>}
